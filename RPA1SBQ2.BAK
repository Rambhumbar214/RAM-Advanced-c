//swap
#include<stdio.h>
#include<conio.h>
#define SWAP(A,B) do{ \
 (A) = (A) + (B); \
 (B) = (A) - (B); \
 (A) = (A) - (B); \
		    }while(0)
int main()
{
int x=10,y=100;
clrscr();
SWAP(x, y);
printf("swaping Number is %d and %d",x,y);
getch();
return 0;
}
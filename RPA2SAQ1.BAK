// swap interchange value of two numbers using pointer
#include<stdio.h>
#include<conio.h>
int main()
{
int a,b,tem;
int*ptr1,*ptr2;
clrscr();
printf("Enter two number");
scanf("%d %d",&a,&b);
ptr1=&a;
ptr2=&b;
printf("before swapping %d and %d\n",a,b);
tem=*ptr1;
*ptr1=*ptr2;
*ptr2=tem;
printf("after swapping numbers is %d and %d\n",a,b);
getch();
return 0;
}